{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],

  "compilerOptions": {
    "baseUrl": "src", // Set the base directory for absolute imports
    "paths": {
      "assets/*": ["assets/*"] // Map "assets" to the "src/assets" folder
    }
  }

}
